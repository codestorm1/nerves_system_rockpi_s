# U-Boot environment configuration for Nerves.Runtime/uboot_env
#
# Keep the byte offset and size in sync with UBOOT_ENV_OFFSET and
# UBOOT_ENV_COUNT in fwup_include/fwup-common.conf.

# Device name  Device offset  Environment size
/dev/mmcblk2   0x00f00000     0x00008000
